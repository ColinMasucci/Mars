Phobos.imu.init();

int i = 0;
while (i < 5) {
    float::m/s^2[] accel = Phobos.imu.getAcceleration();

    print("sample", i);
    print("accel_ms2", accel[0], accel[1], accel[2]);

    wait(0.5);
    i += 1;
}
