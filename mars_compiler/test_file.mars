import keyboard_input;

print("keyboard_input mars test");
print("press ? for instructions");

keyboard_input.print_instructions();

while (true) {
    string key = keyboard_input.read_key();
    if(key != "none") {
    print("key:", key);
    }

    if (key == "quit") {
        break;
    }
}

keyboard_input.cleanup();
