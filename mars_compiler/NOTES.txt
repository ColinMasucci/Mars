#scope is done with a stack of dictionaries done after the AST generation

TODO:
    #methods
    #arrays
    hashmaps
    #classes
    scope
    #multifile support
    better error handling
    external packages
    change colors of our language (Text Mate, LSP Language Server Protocol)

    #use constants instead of strings for the role in shunting yard
    templates and generics for array

    # add ! to requirements

    add command to comment out lines correctly

    if expect_operand:  #Error in parser. 2 repeated if statements that say the same thing. (around line 438)

    Component implementation check failed:
        why is it not flagging for parameters?

    TODO: GET RID OF THE COMPILER ACCESS FOR USERS 
        and update documentation accordingly